(function (ng) {

	'use strict';
	
	/**
	 * @ngdoc module
	 * @name app.ticket
	 * @description
	 *
	 * # app.ticket
	 *
	 * The `app.ticket` module handles
	 * the ticket view of the app.
	 */
	ng.module('app.ticket', []).run(function () {
		console.log( '`app.ticket` init' );
	});

}(window.angular));